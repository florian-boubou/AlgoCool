ALGORITHME Algo
variable :
    x, y : entier
    st   : chaine
    bOk  : booleen
DEBUT
    x  ◄— hasard(10) × 10
    y  ◄— hasard(10)
    x  ◄— x + y
    st ◄— enChaine(x)
    bOk◄— estEntier(x)
    si bOk alors
        st ◄— " est un nombre entier !"
        ecrire(x & st)
    fsi
FIN