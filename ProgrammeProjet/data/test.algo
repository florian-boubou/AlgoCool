ALGORITHME Algo
variable :
    x : entier
DEBUT
    lire(x)
    ecrire("joie")
    ecrire(x)
    ecrire("allegresse")
    si x = 12 alors
        ecrire("x est égal à 12")
        ecrire("Le 5ème va vous étonner")
    sinon
        ecrire("x n'est pas égal à 12")
    fsi
FIN