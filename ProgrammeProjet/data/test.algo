ALGORITHME Algo
variable :
    x, y : entier
    tab : Tableau[3] d'entier
DEBUT
    lire(tab[0])
    tab[1] ◄— 6
    y ◄— tab[0]
    lire(x)
    ecrire("joie")
    ecrire(x)
    ecrire("allegresse")
    si x = 12 alors
        ecrire("x est égal à 12")
        ecrire("Le 5ème va vous étonner")
    sinon
        ecrire("x n'est pas égal à 12")
    fsi
FIN