ALGORITHME Algo
variable :
    y    : entier
    st   : chaine
    bOk  : booleen
DEBUT
    y  <- 5 x 8
    st <- enChaine(y)
    bOk<- estReel(y)
    si bOk alors
        st <- " est un nombre réel !"
        ecrire(y & st)
    sinon
        st <- " est un nombre entier !"
        ecrire(y & st)
    fsi
FIN